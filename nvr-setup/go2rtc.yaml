# go2rtc configuration — 6 Dahua IP cameras
# Each camera is its own device on the LAN (channel=1 for all).
# subtype=1 → sub-stream (H.264, required for Google Home / Chromecast compatibility).
# subtype=0 is the main stream but uses H.265 which Chromecast cannot decode.
# ---------------------------------------------------------------

streams:
  # Cameras serve H.265 even on subtype=1 — transcode to H.264 for browser compatibility
  deck_nosound:  "ffmpeg:rtsp://admin:Mobile99@192.168.1.53:554/cam/realmonitor?channel=1&subtype=1#video=h264"
  edna_mike:     "ffmpeg:rtsp://admin:Mobile99@192.168.1.54:554/cam/realmonitor?channel=1&subtype=1#video=h264"
  bikes:         "ffmpeg:rtsp://admin:Mobile99@192.168.1.51:554/cam/realmonitor?channel=1&subtype=1#video=h264"
  gina:          "ffmpeg:rtsp://admin:admin1234@192.168.1.55:554/cam/realmonitor?channel=1&subtype=1#video=h264"
  entrance:      "ffmpeg:rtsp://admin:Mobile99@192.168.1.50:554/cam/realmonitor?channel=1&subtype=1#video=h264"
  deck_sound:    "ffmpeg:rtsp://admin:Mobile99@192.168.1.52:554/cam/realmonitor?channel=1&subtype=1#video=h264"

# Listen on all interfaces — CORS is handled by nginx in front of go2rtc
api:
  listen: ":1984"

log:
  level: info
